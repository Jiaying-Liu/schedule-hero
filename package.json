{
    "name": "deployment",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "start": "react-scripts start",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
      "node": "8.9.4",
      "npm": "5.6.0"
    }
  }